# ============================================
# v8.3 红利策略监测 - Python依赖（版本锁定）
# ============================================
# 锁定版本原因：避免库更新导致脚本不兼容（长期运行稳定性）

# 数据获取（备用数据源，GitHub Actions跨境可用）
yfinance==0.2.40

# 数据处理与科学计算
pandas==2.2.3
numpy==1.26.4

# YAML配置解析
pyyaml==6.0.2

# HTTP请求（Webhook推送 + 腾讯/新浪数据接口）
requests==2.32.3

# 日期时间处理
python-dateutil==2.9.0

# AKShare（非必需，国内环境备用）
# 注意：GitHub Actions环境下AKShare经常超时，已降级为P3数据源
# 如需使用，取消下行注释：
# akshare==1.14.38
